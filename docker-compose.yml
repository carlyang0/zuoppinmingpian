services:
  portfolio-card:
    build: .
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 8787
      HOST: 0.0.0.0
      DATA_DIR: /data
      BASE_URL: https://your-domain.com
    volumes:
      - ./data:/data
    ports:
      - "127.0.0.1:8787:8787"
